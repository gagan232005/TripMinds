{
  "name": "tripminds",
  "version": "1.0.0",
  "description": "TripMinds — budget-first multi-agent AI travel planner (V1). Travel smarter. Spend better.",
  "type": "commonjs",
  "main": "server.js",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "build:web": "npm --prefix web install && npm --prefix web run build"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.2"
  }
}
